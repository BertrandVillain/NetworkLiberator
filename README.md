# NetworkLiberator
A little software to scan the local network and block a selection of IP

#The software have been made for Linux (test only with ubuntu 16)
To make it work please install the following packages:
- sudo apt-get install python3
- sudo pip install scapy-python3
- sudo apt-get install python3-tk
Possible missing ones ...
